Step 1: Build and Run in Developer Mode

Step 2: Recreate the Bug

Step 3: Find and read unread-anywhere tsx file

Step 4: "Move the API call and tab opening to the background.ts. The content script just wraps the message + UI part"

Just try to work on it and open a PR if you can fix it. However last time I tried to do this I ran into some issues, probably due to cookies (I don't remember). See main...unread-in-backgr0und

# Test Cases for Issue #8667 Fix

## Manual Test Cases

### Test Case 1: Bug Reproduction (Main Fix Validation)
**Purpose:** Verify that tabs open even when the originating tab is closed quickly

**Prerequisites:**
- Have at least 1 unread GitHub notification
- Extension loaded and enabled

**Steps:**
1. Navigate to any GitHub page (e.g., github.com/refined-github/refined-github)
2. Click the "Open unread notifications" button (arrow icon next to notifications bell)
3. **IMMEDIATELY** close the tab (within 0.5 seconds)

**Expected Result:**
- ✅ New tabs open with unread notifications (1-5 tabs)
- ✅ Tabs appear even though the original tab was closed

**Actual Result (Before Fix):**
- ❌ No tabs opened
- ❌ Background script never received the URLs

**Status:** [ ] Pass [ ] Fail

---

### Test Case 2: Normal Operation (No Tab Close)
**Purpose:** Verify the feature still works normally when tab isn't closed

**Prerequisites:**
- Have 2-3 unread GitHub notifications
- Extension loaded and enabled

**Steps:**
1. Navigate to any GitHub page
2. Click the "Open unread notifications" button
3. Wait for completion (don't close tab)

**Expected Result:**
- ✅ Toast shows "Loading notifications…"
- ✅ Toast updates to "X notifications opened"
- ✅ 2-3 new tabs open in background
- ✅ Button becomes enabled again
- ✅ Unread indicator (dot) removed from notifications bell

**Status:** [ ] Pass [ ] Fail

---

### Test Case 3: Zero Unread Notifications
**Purpose:** Verify graceful handling when no notifications exist

**Prerequisites:**
- Have ZERO unread notifications (mark all as read)
- Extension loaded and enabled

**Steps:**
1. Navigate to any GitHub page
2. Click the "Open unread notifications" button
3. Observe the toast message

**Expected Result:**
- ✅ Toast shows "Loading notifications…"
- ✅ Toast updates to "No unread notifications"
- ✅ No new tabs open
- ✅ Button becomes enabled again

**Status:** [ ] Pass [ ] Fail

---

### Test Case 4: Many Notifications (>5)
**Purpose:** Verify the 5-notification limit is respected

**Prerequisites:**
- Have 10+ unread GitHub notifications
- Extension loaded and enabled

**Steps:**
1. Navigate to any GitHub page
2. Click the "Open unread notifications" button
3. Count the number of tabs that open

**Expected Result:**
- ✅ Exactly 5 new tabs open
- ✅ Toast shows "Opened the last 5 unread notifications"
- ✅ Button becomes enabled again

**Status:** [ ] Pass [ ] Fail

---

### Test Case 5: Network Error Handling
**Purpose:** Verify graceful error handling when network fails

**Prerequisites:**
- Extension loaded and enabled
- Ability to disable network or block GitHub

**Steps:**
1. Open DevTools Network tab
2. Enable "Offline" mode
3. Click the "Open unread notifications" button
4. Observe behavior

**Expected Result:**
- ✅ Toast shows "Loading notifications…"
- ✅ Toast updates to "Failed to fetch notifications"
- ✅ No tabs open
- ✅ Error logged to console
- ✅ Button becomes enabled again

**Status:** [ ] Pass [ ] Fail

---

### Test Case 6: PR Files Tab Filtering
**Purpose:** Verify that links to PR "Files" tabs are filtered out

**Prerequisites:**
- Have an unread notification that points to a PR's Files tab
- Extension loaded and enabled

**Steps:**
1. Create a PR and get a notification that would link to /files
2. Click the "Open unread notifications" button
3. Check opened tabs

**Expected Result:**
- ✅ PR Files tab URLs are filtered out
- ✅ Only non-/files URLs open

**Status:** [ ] Pass [ ] Fail

---

### Test Case 7: Keyboard Shortcut (g + u)
**Purpose:** Verify the hotkey still works with new implementation

**Prerequisites:**
- Have 1+ unread notifications
- Extension loaded and enabled

**Steps:**
1. Navigate to any GitHub page
2. Press `g` then `u` (keyboard shortcut)
3. Immediately close the tab

**Expected Result:**
- ✅ Tabs open even though tab was closed
- ✅ Same behavior as clicking the button

**Status:** [ ] Pass [ ] Fail

---

### Test Case 8: Multiple Rapid Clicks
**Purpose:** Verify button disabling prevents duplicate requests

**Prerequisites:**
- Have 3+ unread notifications
- Extension loaded and enabled

**Steps:**
1. Navigate to any GitHub page
2. Click the button rapidly 3 times
3. Observe behavior

**Expected Result:**
- ✅ Button becomes disabled after first click
- ✅ Only one set of tabs opens (not 3 sets)
- ✅ Button re-enables after completion

**Status:** [ ] Pass [ ] Fail

---

### Test Case 9: Console Error Check
**Purpose:** Verify no errors are thrown during execution

**Prerequisites:**
- Extension loaded and enabled
- DevTools Console open

**Steps:**
1. Open DevTools (F12) → Console tab
2. Click the "Open unread notifications" button
3. Monitor console for errors

**Expected Result:**
- ✅ No TypeScript errors
- ✅ No runtime errors
- ✅ Only expected log messages (if any)

**Status:** [ ] Pass [ ] Fail

---

### Test Case 10: Tab Index/Positioning
**Purpose:** Verify tabs open in correct position (next to current tab)

**Prerequisites:**
- Have 2+ unread notifications
- Multiple tabs already open

**Steps:**
1. Open 5 tabs in browser
2. Navigate to tab #3
3. Go to any GitHub page
4. Click the "Open unread notifications" button
5. Observe where new tabs appear

**Expected Result:**
- ✅ New tabs open immediately after the current tab (positions 4, 5, etc.)
- ✅ New tabs open in background (not focused)
- ✅ Original tab remains focused

**Status:** [ ] Pass [ ] Fail

---

## Automated Test Cases (For Future Implementation)

### Unit Test 1: Message Handler Returns Correct Structure
```typescript
test('fetchAndOpenUnreadNotifications returns success structure', async () => {
  const result = await handleFetchAndOpenUnreadNotifications();
  expect(result).toHaveProperty('success');
  expect(result).toHaveProperty('count');
  expect(result).toHaveProperty('message');
});
```

### Unit Test 2: Handles Zero Notifications
```typescript
test('returns count 0 when no notifications', async () => {
  // Mock fetch to return empty notifications page
  const result = await handleFetchAndOpenUnreadNotifications();
  expect(result.success).toBe(true);
  expect(result.count).toBe(0);
  expect(result.message).toBe('No unread notifications');
});
```

### Unit Test 3: Limits to 5 Notifications
```typescript
test('limits to 5 tabs even with 10 notifications', async () => {
  // Mock fetch to return 10 notifications
  const result = await handleFetchAndOpenUnreadNotifications();
  expect(result.count).toBe(5);
  expect(result.total).toBe(10);
});
```

### Unit Test 4: Filters PR Files URLs
```typescript
test('filters out /files URLs', async () => {
  // Mock notifications with /files URLs
  const result = await handleFetchAndOpenUnreadNotifications();
  // Verify opened URLs don't contain /files
});
```

### Unit Test 5: Handles Fetch Errors
```typescript
test('handles network errors gracefully', async () => {
  // Mock fetch to throw error
  const result = await handleFetchAndOpenUnreadNotifications();
  expect(result.success).toBe(false);
  expect(result.message).toBe('Failed to fetch notifications');
});
```

---

## Test Execution Checklist

**Pre-Testing:**
- [ ] Extension built successfully (`npm run build`)
- [ ] Extension loaded in Chrome
- [ ] No console errors on load
- [ ] DevTools open for monitoring

**During Testing:**
- [ ] Complete all 10 manual test cases
- [ ] Document any failures
- [ ] Note any unexpected behavior
- [ ] Check console after each test

**Post-Testing:**
- [ ] All tests pass
- [ ] No console errors observed
- [ ] Feature works as expected
- [ ] Bug is fixed (Test Case 1 passes)

---

## Test Results Summary

| Test Case | Status | Notes |
|-----------|--------|-------|
| TC1: Bug Fix | [ ] | |
| TC2: Normal Operation | [ ] | |
| TC3: Zero Notifications | [ ] | |
| TC4: Many Notifications | [ ] | |
| TC5: Network Error | [ ] | |
| TC6: PR Files Filter | [ ] | |
| TC7: Keyboard Shortcut | [ ] | |
| TC8: Rapid Clicks | [ ] | |
| TC9: Console Check | [ ] | |
| TC10: Tab Positioning | [ ] | |

**Overall Status:** [ ] Pass [ ] Fail

**Tested By:** ___________________
**Date:** ___________________
**Browser:** Chrome Version: ___________________
**Extension Version:** ___________________

---

## For Your HW6b Report

Include:
- Screenshot of Test Case 1 (the main bug fix) working
- Screenshot of console showing no errors
- Test results summary table
- Description of testing methodology
- Any edge cases discovered during testing
